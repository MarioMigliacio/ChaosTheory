#include "Settings.h"

namespace Settings {
    sf::Vector2u resolution = { 800, 600 };
    bool fullscreen = false;

    void Load() {
        // TODO: Load settings from file (e.g., JSON)
    }

    void Save() {
        // TODO: Save settings to file
    }
}